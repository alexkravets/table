service: storage

provider:
  stage:   test
  region:  localhost
  profile: private

dynamodb:
  endpoint: http://0.0.0.0:8000
  ProvisionedThroughput:
      ReadCapacityUnits:  1
      WriteCapacityUnits: 1
